services:
  db:
    image: postgres:latest
    container_name: bdBaloo
    environment:
      POSTGRES_DB: bdBaloo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgresql
    ports:
      - '5433:5432'
    volumes:
      - ./postgres_data:/var/lib/postgresql